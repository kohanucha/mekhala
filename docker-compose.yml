services:
  nwc-relay:
    build: .
    ports:
      - "7777:7777"
    volumes:
      - nwc-data:/data
    environment:
      - RELAY_PORT=7777
      - RELAY_NAME=My Private NWC Relay
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  nwc-data:
