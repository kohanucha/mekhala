name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Install worker-build
        run: |
          if ! command -v worker-build &> /dev/null; then
            cargo install worker-build
          fi

      - name: Inject Git Metadata
        run: |
          VERSION="${{ github.ref_name }}-${GITHUB_SHA:0:7}"
          SOFTWARE="${{ github.server_url }}/${{ github.repository }}"
          sed -i "s|^RELAY_SOFTWARE = .*|RELAY_SOFTWARE = \"$SOFTWARE\"|" wrangler.toml
          sed -i "s|^RELAY_VERSION = .*|RELAY_VERSION = \"$VERSION\"|" wrangler.toml

      - name: Build & Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          wranglerVersion: '4'
          command: deploy
